TopSecret
2/4/26
Claudia Castagna

This class controls the command line interface for the user. When the program runs with no arguments a list
of available files should be printed to the screen. When the program runs with just a number as an argument
the contents of the corresponding file should be displayed on the screen. When no second parameter is
given the program uses the default key. If a second parameter is specified this indicates which cipher to
use to decode the file.

Userinterface
-------------------------------
+String determineOutput(String[])

Userinterface Pseudocode:
-------------------------------

determineOutput(num_args)

    if num_args == 0:
        try:
            list all files available
        catch: //catch propagated error
            print "Error accessing files"

    else if num_args == 1:
        try:
            decipher with default key
        catch:
            print "Invalid argument, first argument must be an int"

        catch:
            print "Error accessing files"


    else if num_args == 2:
        try:
            decipher with specified key
        catch:
            print "Invalid argument, first argument must be an int and second a String"
        catch:
            print "Error accessing files"

    else:
        throw illegal argument exception "too many arguments called with program "








